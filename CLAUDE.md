# Work Management — 작업 규칙

> **업무관리툴은 경영지원부장과 AI가 함께 일하는 작업장이며, 그 일하는 방식이 데이터로 남아 나중에 AI가 넘겨받을 수 있게 하는 곳이다.**
> **밖으로 나가는 것은 사용자가 「공유」를 누른 것뿐이다.**

사용자는 경영지원부장 한 명 + AI. 나중에 사업체가 5개까지 늘어날 수 있다.

React + Vite + TypeScript + Tailwind v4 프론트엔드가 Supabase에 직접 붙는다. 별도 백엔드 서버는 없다. AI 호출만 Supabase Edge Function을 거친다.

**이 저장소는 독립 프로젝트다.** `Desktop/WorkManagement/`가 저장소 루트이고, 경영관리서비스(`Desktop/jeus/`)와는 완전히 분리되어 있다. 두 프로젝트의 파일을 서로 옮기지 않는다.

**이 제품이 남겨야 하는 것은 「무엇을 했나」가 아니라 「어떻게 했나」와 「왜 그렇게 했나」다.** 제목·상태·기한만 남기는 할일 앱은 이미 많다. 그 두 층을 만드는 게 이 제품의 존재 이유이므로, 어떤 기능을 짜든 *"이 기능이 절차·실행이력·예외를 남기는가"*를 먼저 확인한다.

## 문서 위치

| 문서 | 위치 | 역할 |
|---|---|---|
| 설계서 | `docs/specs/` | **무엇을 왜 만드는가** — 원본 |
| 연동 계약서 | `docs/specs/` | 경영관리서비스와 주고받는 것의 필드 단위 정의 |
| 구현 계획 | `docs/plans/` | 어떤 순서로 만드는가 |
| **회고** | `docs/retro/` | **그때 어떤 상황이었고 왜 그렇게 판단했는가** |

**현재 유효한 설계서는 `docs/specs/2026-08-09-work-management-design-v1.md`다.**

무엇을 왜 만드는지는 **설계서가 원본**이다. 구현 중 설계와 다른 판단이 필요하면 설계서를 먼저 고치고, **그 판단의 경위를 회고에 남긴다.**

---

## 제품 경계 — 넘지 않는다

경영관리서비스(`Desktop/jeus/`)와 역할이 갈린다. **이 경계를 어기는 기능은 만들지 않는다.**

| | **업무관리툴** (여기) | **경영관리서비스** |
|---|---|---|
| 소유 | 업무 · **절차** · **실행이력** · **예외** · **문서** · **AI 대화** · 일지 · 인박스 · 회의록 · 지시사항 | 숫자(손익·재고·현금) · 이슈 · 보고서 · 대표 알림 |
| 기본 공개 | **비공개** — 자동으로 나가는 것이 없다 | 관련자에게 공개 |

**하지 않는 것 (설계서 §1.3)**

- **숫자를 집계하지 않는다.** 손익·재고·현금은 경영관리서비스의 것이다. 여기는 "그 숫자를 만드는 일"만 관리한다
- **이카운트·위하고와 연동하지 않는다.** 외부 시스템 연동은 경영관리서비스가 한다
- **대표·담당자가 회사 숫자를 묻는 창구를 만들지 않는다.** 여기의 챗봇은 **사용자 본인이 자기 업무를 묻는 곳**이다
- **협업 기능을 만들지 않는다.** 단독 사용이 전제다

**두 툴 사이로 오가는 것은 이것뿐이다** (상세는 연동 계약서)

```
나가는 것 ── 이슈 발행 (사용자가 「이슈로 공유」를 누를 때만)
             제목 · 핵심 내용 · 사실/추정 · 재무영향 · 요청 결정 · 근거
             + 「대표에게 바로 알리기」 요청 (긴급도 · 사유)
             ✕ 업무 제목 · 진행률 · 체크리스트 · 일지 · 절차 · 실행이력
             ✕ 소요시간 · AI 대화 내용

들어오는 것 ─ 대표 지시(확정건) → 인박스 건너뛰고 바로 업무, 중요도 P0
             이슈 회신 · 배정      → 인박스로
```

**경영관리서비스도 React + Supabase로 만든다.** 스택이 같지만 **Supabase 프로젝트는 서로 다르다.** DB를 공유하지 않고 Edge Function끼리 주고받는다.

**주간·월간 리포트는 밖으로 나가지 않는다.** 사용자 본인의 모니터링과 연말 성과평가용 내부 자료다.

---

## AI 규칙

이 저장소에도 AI가 들어간다. 다만 **사용자 본인의 작업 도우미**이지 대표·담당자의 질문에 답하는 창구가 아니다. 그건 경영관리서비스의 몫이다.

**하는 일 5가지**

| # | | |
|---|---|---|
| **0** | **업무 비서 (챗봇)** ⭐ | 일하는 중에 옆에서 묻고 답한다. **그 대화에서 절차가 자란다** |
| 1 | 문서 초안 | 공지·공문·행정 신고 서류 |
| 2 | 절차(SOP) 추출 | 3회 반복 감지 후 초안 제안 |
| 3 | 예외 설명 | 탐지된 이상에 "왜 달랐나"를 붙임 |
| 4 | 회의록 요약·용어 교정 | 전사문 → 회의록 초안 |

### 지켜야 할 것

- **AI는 초안까지, 확정은 사람이 한다.** 어떤 기능도 사용자 승인 없이 결과를 확정하거나 발송하지 않는다. 행정 신고 서류는 틀리면 실제 불이익이 난다
- **계산으로 되는 것은 AI에 맡기지 않는다.** 예외 **탐지**(규칙 6종)와 절차 후보 **감지**(3회 반복)는 `src/domain/`의 순수 함수다. AI는 그 위에 **문장만 얹는다**
  - 이유: 테스트할 수 있고, 비용이 0이고, **AI가 죽어도 동작한다**
- **AI 호출이 실패해도 업무·절차·실행이력 기록은 정상 동작해야 한다.** AI는 부가 기능이다
- **초안과 확정본을 둘 다 저장한다.** 그 차이가 "AI가 뭘 놓쳤나"를 알려주는 유일한 신호다. 매번 같은 곳을 고치고 있으면 프롬프트나 양식이 잘못된 것이다
- **AI 답변에는 근거 출처를 표시한다.** 어느 절차·실행이력·문서를 보고 한 말인지 없으면 사용자가 믿을 수 없다. 근거가 없으면 "모른다"고 답한다
- **업무 비서가 절차를 마음대로 고치지 않는다.** 항상 `[예][아니오]`로 물어본다
- **업무 비서는 회사 숫자를 다루지 않는다.** "이번 달 재고 얼마야?"에는 "이 툴이 다루지 않는다"고 답한다

### AI 공급자 — 갈아끼울 수 있게 둔다

**현재 공급자는 OpenAI다.** 원래 Claude를 쓰려 했으나 호출이 반복 실패해서 바꿨다(설계서 §14 OQ-11 — 원인 미규명).

**그래서 어댑터를 한 겹 뒀다.** `supabase/functions/_shared/provider/` 아래에서 공급자를 감싸고, 화면 코드는 어느 회사 AI인지 모른다.

- **공급자를 바꿔도 화면·DB를 안 고친다.** 어댑터 파일 + 환경변수만 바꾼다
- **모델명을 코드에 박지 않는다.** 환경변수 `AI_MODEL`로 둔다
- 모델을 정하면 **설계서 §8에 기록한다** (OQ-12)

### API 키

**AI API 키를 프론트엔드에 넣지 않는다.** Supabase Edge Function을 거친다.

```
브라우저 → Supabase Edge Function → AI 공급자
           (여기에만 키가 있다)
```

빌드 산출물에 키 문자열이 들어갔는지 확인하는 것이 수용 기준에 있다.

---

## 핵심 용어 — 헷갈리면 안 된다

| 용어 | 테이블 | 무엇인가 |
|---|---|---|
| **업무** | `tasks` | *이번에 무엇을 했나* |
| **절차** | `procedures` | *이런 일은 이렇게 한다* — 트리거 · 입력자료 · 단계 · **판단기준** · 산출물 |
| **실행 / 회차** | `runs` | 절차를 이번에 실제로 돌린 기록 |
| **예외** | `exceptions` | 절차대로 안 됐을 때 — **가장 값진 데이터** |
| **문서** | `documents` | 절차의 산출물. AI 초안 + 확정본 |
| **대화** | `assistant_threads` | AI 업무 비서와 오간 기록. 여기서 절차가 자란다 |
| **발행** | `issue_publications` | 경영관리서비스로 내보낸 이슈 |

**반복업무는 별도 개념이 아니다.** *트리거가 「주기」인 절차*다. `routines` 같은 테이블을 새로 만들지 않는다.

**절차는 사람의 추가 노동이 아니라 부산물로 나와야 한다.** 아는 업무는 직접 작성하고, 모르던 업무는 같은 영역·유사 제목이 3건 쌓이면 툴이 초안을 제안한다. "절차를 문서로 쓰세요"라고 요구하는 UI를 만들지 않는다.

**예외도 사람이 적지 않는다.** 툴이 탐지하고 사람은 확인만 한다.

---

## 로드맵 — 지금 어디인가

**7단계.** 각 단계가 끝나면 그 시점부터 실제로 쓸 수 있어야 한다. (상세는 설계서 §13)

| | 단계 | 상태 |
|---|---|---|
| 1 | 뼈대와 업무 — 스키마·RLS·인증·6탭·칸반·지시사항·인박스·시드 | **진행 중** |
| 2 | 절차와 실행이력 ⭐ | |
| 3 | **AI 비서와 문서** ⭐ | |
| 4 | 하루의 흐름 (오늘 화면·업무일지) | |
| 5 | 예외와 리포트 | |
| 6 | 경영관리서비스 연동 | |
| 7 | 계획·기준·녹음 | |

**2·3단계를 앞에 둔 이유가 이 로드맵의 핵심이다.** 절차와 실행이력이 없으면 아무리 오래 써도 학습 데이터가 안 쌓인다. 「오늘」 화면이 없어도 업무는 굴러가지만, 절차가 없으면 6개월을 써도 남는 게 제목과 완료일뿐이다. **데이터가 쌓이기 시작하는 시점을 최대한 앞당긴다.**

**AI는 3단계에서 처음 들어온다.** 그 전까지는 AI 없이 동작한다. 챗봇을 2단계보다 먼저 둘 수 없는 이유는 절차와 실행이력이 있어야 "지난번엔 이렇게 하셨습니다"를 말할 수 있기 때문이다.

---

## 회고 — 커밋할 때마다 쓴다

**파일명:** `docs/retro/YYYY-MM-DD_vNN.md` — 예: `2026-08-09_v01.md`

하루에 여러 번 커밋하면 `v01`, `v02`, `v03`으로 올라간다.

### 회고는 개발일지가 아니다

무엇을 만들었는지는 커밋 로그를 보면 된다. 회고가 남겨야 하는 것은 **"왜 이렇게 되어 있는가"에 대한 답**이다.

몇 달 뒤에 "이 코드 왜 이래?", "왜 이 라이브러리를 골랐지?", "왜 이 폴더가 여기 있지?" 하는 질문이 생겼을 때 **근거 문서**로 쓰인다. 그래서 잘된 것만 적지 않는다. **틀렸던 것, 착각했던 것, 아직 안 풀린 것**을 같이 적는다.

### ★ 비개발자가 읽을 수 있어야 한다

**이 회고의 독자는 개발자가 아니다.** 3개월 뒤의 본인과, 이 프로젝트를 처음 보는 사람이 읽고 "아 이런 상황이었고 이렇게 풀었구나"를 알 수 있어야 한다.

- **개발 용어는 그 자리에서 괄호로 푼다.** `데이터베이스(정보를 저장하는 곳)`, `커밋(작업 기록 저장)`, `git(작업 기록을 순서대로 보관하는 장치)`
- **테이블·필드·함수명을 그대로 쓰지 않는다.** `tasks 테이블` → 「업무 저장 공간」, `procedures` → 「절차」
- **코드를 붙이지 않는다.** 무슨 일이 일어났는지를 말로 설명한다
- 표·화살표 그림·인용을 적극적으로 쓴다. **줄글이 길면 안 읽힌다**

### 섹션 구성

| 순서 | 섹션 | 무엇을 쓰나 |
|---|---|---|
| 1 | 📌 **오늘 한 일 (요약)** | 불릿 목록. 이것만 읽어도 그날이 파악돼야 한다 |
| 2 | 🌅 **오늘의 출발점** | 어떤 자리에서 시작했나. 그때 이미 있던 것, 무엇이 아쉬웠나 |
| 3 | 🛠 **작업 상세** | 항목마다 **무엇을 / 왜 / 결과** 3단 (아래 참고) |
| 4 | 😵 **어려웠던 점 & 해결** | ★ 가장 중요하다. **5단 구성**을 지킨다 (아래 참고) |
| 5 | 🔀 **갈림길에서 무엇을 왜 골랐나** | 고른 것 / 버린 것 / 결정적 이유 / **치른 대가** |
| 6 | 📋 **남은 것과 다음** | 다음 할 일 + **아직 안 풀린 구조적 문제** + 일부러 안 하기로 한 것 |
| 7 | 📚 **오늘 배운 것** | 주제 + 한 줄 요약. 급할 때 이것만 봐도 되게 |

커밋 SHA와 제목은 1번 섹션 끝에 적는다.

### 작업 상세는 3단으로 쓴다

```
무엇을  — 무엇을 만들었거나 바꿨나 (용어를 풀어서)
왜      — 그전엔 뭐가 문제였나
결과    — 이제 무엇이 가능해졌나 / 무엇이 달라졌나
```

**「왜」를 빼먹지 않는다.** 무엇을 했는지만 적으면 커밋 로그와 다를 게 없다.

### 이슈는 5단으로 쓴다

```
상황     — 무엇을 하던 중이었나
증상     — 무엇이 보였나 (오류 메시지는 그대로, 대신 무슨 뜻인지 풀어서)
원인     — 왜 그랬나. 여기서 멈추지 말 것
해결     — 어떻게 고쳤고, 왜 그 방법이었나
남은 문제 — 고친 뒤에 새로 생긴 것 / 아직 안 풀린 것
```

**「남은 문제」를 빼먹지 않는다.** 대부분의 수정은 다른 것을 남긴다. 그걸 안 적으면 다음 사람이 같은 자리를 다시 판다.

**「원인」에서 멈추지 않는다.** "왜 그랬나"의 답이 "왜 그런 구조였나"까지 가야 한다.

### 원칙

- **이전 버전은 절대 수정하지 않는다.** 앞선 판단이 틀렸다고 밝혀져도 그 파일은 그대로 두고, 다음 버전에 "v01에서 이렇게 했는데 이런 이유로 바꿨다"를 적는다. 회고는 결론이 아니라 **경위 기록**이다.
  - 예외: 내용을 바꾸지 않는 **형식 정리**는 허용한다. 대신 문서 머리에 "언제 무엇을 정리했다"를 밝힌다.
- **잘된 것만 적지 않는다.** 틀렸던 것·착각했던 것·아직 안 풀린 것을 같이 적는다. 그게 이 문서의 값어치다.
- **실명을 쓰지 않는다.** 역할로 적는다 — "구매사업본부 담당자", "대표". 저장소는 외부 서버에 올라간다.
- 이슈가 없었으면 "없음"이라고 적는다. 억지로 만들지 않는다.

### 이렇게 쓴다 — 예시

**용어 풀기**

| ✕ 이렇게 쓰지 않는다 | ○ 이렇게 쓴다 |
|---|---|
| `tasks` 테이블에 `focus_date` 컬럼 추가 | 업무 저장 공간에 「오늘 할 일로 찍은 날짜」 항목을 추가 |
| RLS 정책을 적용했다 | 남의 데이터가 안 보이게 막는 장치(RLS)를 켰다 |
| `TS2593` 오류로 빌드가 깨졌다 | 검사 도구가 테스트 파일의 명령어를 못 알아봐서 빌드가 실패했다 |
| Edge Function으로 프록시했다 | 중간에 서버를 하나 두고 그걸 거쳐 호출하게 했다 |

**작업 상세 3단**

```markdown
### 3. 반복업무를 「절차」에 흡수 (저장 공간 2개 삭제)

- **무엇을:** 매일·매주 체크하는 「반복업무」를 별도 개념으로 두지 않고,
  주기가 정해진 절차로 합쳤습니다. 담으려던 저장 공간 2개가 사라졌습니다.
- **왜:** 구조가 똑같았습니다. "반복업무 + 체크 기록"과 "절차 + 실행이력"이
  사실상 같은 것이었습니다.
- **결과:** 매일 체크하던 행위 자체가 학습 데이터가 됩니다. 따로 절차 문서를
  쓸 필요가 없어졌습니다.
```

**이슈 5단**

```markdown
### 🔴 2. 두 프로젝트가 같은 것을 두 번 만들고 있었습니다

- **상황:** 두 툴의 경계를 설계하려고 상대편 설계서를 읽었습니다.
- **증상:** 저쪽 「업무관리 탭」이 이쪽 계획과 정확히 겹쳤습니다. (표로 대조)
- **원인:** 두 프로젝트가 각자 따로 설계됐기 때문입니다. 어느 쪽도 틀리지
  않았습니다 — 아무도 경계를 안 그었을 뿐입니다.
- **해결:** 세 방안 중 A(업무는 이쪽 소유)로 정했습니다. 이미 만든 것이
  살아남고, 저쪽 설계서의 내부 모순도 함께 풀리기 때문입니다.
- **남은 문제:** ⚠️ 아직 합의된 게 아닙니다. 저쪽 설계가 굳으면 중복이
  확정됩니다.
```

**표현 톤** — 설명 부분은 `~습니다`체를 쓴다. 읽는 사람이 개발자가 아니기 때문이다.

---

## 보안 — 타협하지 않는다

- **`service_role` 키를 코드나 `.env`에 절대 넣지 않는다.** RLS를 통째로 우회하는 마스터 키다. 브라우저에 들어가는 건 `anon` 키뿐이다.
- **`.env.local`은 커밋하지 않는다.** `.gitignore`에 이미 있다.
- **모든 테이블에 RLS를 켠다.** 안 켜면 `anon` 키를 가진 누구나 전체 데이터를 읽는다.
- **사업이해자료(「내부 자료 · 사외 반출 금지」)를 이 저장소에 들이지 않는다.** 그 문서는 `Desktop/jeus/`에 있고 이 저장소와 아무 관계가 없다. 인용이 필요하면 **내용을 옮겨 적지 말고 조항 번호만** 참조한다(예: "§9-1").
- **실명을 커밋하지 않는다.** 동료·거래처 이름은 역할·부서로 바꿔 적는다. 시드 데이터도 마찬가지다.
- **금액·수치를 시드 데이터에 넣지 않는다.** 확정 회계수치가 아니라 원장·실사로 대사해야 할 참고값이고, 애초에 경영관리서비스가 다룰 영역이다.
- **회의 녹음 파일과 전사문은 커밋하지 않는다.** `.gitignore`에 `*.m4a`, `*.mp3`, `recordings/`, `transcripts/`가 있다.
- **실시간 받아쓰기(기록 › 회의록 › 🎙 실시간)는 음성이 브라우저 제조사 서버로 나간다.** 그래서 ① 자동으로 켜지지 않고 ② 무엇이 나가는지 화면에 상시 표시한다. 이 둘은 빼지 않는다.
- ⚠️ **민감 회의(회생·인사)를 막지 않는다 — 2026-08-16 사용자 판단.** 원래 규칙은 「잠그고, 전사문을 Supabase에 저장하지 않는다」였으나 *"민감회의도 일반회의와 같은 루트로, 보안은 나중에"* 로 정했다. **「민감」은 표시로만 남긴다** — 나중에 정책을 다시 세울 때 그 표시로 골라내기 위해서다. **표시를 없애지 않는다.** 설계서 §5.8 · OQ-16.

### 공개 정책 — 사용자 업무가 새어 나가는 걸 막는다

사용자의 업무는 비공개인데 AI는 그 데이터로 학습한다. *"이 건에 3주 썼다"* 가 대표에게 새어 나갈 수 있다.

**학습에는 전부 쓰되, AI가 답변 근거로 인용할 수 있는 것은 아래뿐이다.**

| 인용 가능 | 인용 불가 |
|---|---|
| `issue_publications` (발행한 것) | `procedures` · `procedure_steps` (업무 노하우) |
| `directives` (대표가 준 것) | `runs` · `run_steps` · `exceptions` ← **소요시간이 새는 자리** |
| `documents` — **확정본 중 발송·제출한 것만** | `tasks` · `checklist` · `daily_logs` · `document_templates` |
| `periodic_reports` — **사용자가 확정한 것만** | **`assistant_threads` · `assistant_messages` ← 절대 불가.** 내가 AI에게 뭘 물었는지가 가장 사적인 기록이다 |
| | `meetings` · `calls` ← **남이 한 말이 토씨까지 남는다.** 상대가 공개에 동의한 적이 없다 |

**이 표는 문서 주석이 아니라 `disclosure_policy` 테이블로 DB에 넣는다.** 나중에 AI를 붙이는 사람이 이 문서를 안 읽어도 사고가 안 나야 한다.

---

## 코드 규칙

### `src/domain/`은 순수하게 유지한다

**Supabase와 React를 import하지 않는다.** 여기 들어가는 것 —

날짜 계산 · 정렬 · 지시사항 집계 · 인박스 승격 · **예외 탐지** · **절차 후보 감지** · **주기 트리거 계산** · **리포트 집계**

DB나 브라우저 없이 테스트하기 위해 분리한 것이다. 이 경계가 무너지면 테스트가 느려지고 깨지기 쉬워진다.

**판단이 필요 없는 것은 전부 여기로 온다.** AI를 부르기 전에 *"이거 계산으로 되나?"* 를 먼저 묻는다.

### 스냅샷은 굳으면 안 변한다

업무일지의 「오늘 진행한 업무」와 주간·월간·연간 리포트는 **확정 시점의 스냅샷(JSON)으로 저장한다.** 나중에 업무를 수정해도 지난 일지·리포트는 변하지 않는다.

연말 성과평가에서 *"3월에 뭐 했더라"* 를 되짚을 수 있어야 하기 때문이다. 조인해서 실시간으로 다시 계산하는 구현은 이 요구를 깨뜨린다.

### 날짜 — 실제로 결함이 났던 곳이다

- **테스트 픽스처는 로컬 벽시계 리터럴을 쓴다.** `new Date(2026, 7, 11, 9, 0)` ○ / `new Date('2026-08-11T09:00:00+09:00')` ✕
  ISO 문자열은 절대 시각을 고정하는데 구현은 로컬 달력일을 본다. UTC 오프셋이 음수인 지역(미국 등)에서 하루가 밀려 테스트가 깨진다.
- **`daysUntil`은 `Math.floor`가 아니라 `Math.round`를 쓴다.** 서머타임 전환 때 두 로컬 자정 사이가 23시간 또는 25시간이 되는데, `round`가 그 ±1시간을 흡수한다.
- **Windows의 Node는 `TZ` 환경변수를 무시한다.** `TZ=America/New_York npm test`로 시간대 테스트를 했다고 믿으면 안 된다. 실제로는 로컬 시간대로 돌아간다.

### Tailwind v4

**`tailwind.config.js`를 만들지 않는다.** v4는 CSS의 `@theme` 블록으로 토큰을 정의한다. `src/index.css`에 있다.

토큰이 `dist` CSS에 안 보인다고 고장난 게 아니다. v4는 **실제로 쓰인 토큰만 방출한다**. 그리고 Lightning CSS가 색을 줄여 쓰므로 `#0066cc`는 `#06c`로 나온다.

### TypeScript

`verbatimModuleSyntax: true`가 켜져 있다. 타입만 가져올 때는 `import type`을 쓴다.

---

## 디자인 — `DESIGN-apple.md` 기준

- **Action Blue `#0066cc`가 유일한 인터랙티브 색이다.** 두 번째 액센트를 만들지 않는다. 기한 초과 경고(`#d70015`)만 예외로 두되 그것도 최소한으로 쓴다.
- **본문은 17px다.** 16px이 아니다.
- **weight 사다리는 300 / 400 / 600 / 700. 500은 쓰지 않는다.**
- 버튼은 pill, 유틸리티 카드는 radius 18px.
- **카드·버튼·텍스트에 그림자를 넣지 않는다.** 밝은 면과 어두운 면의 교차가 구분선 역할을 한다.
- 잉크는 `#1d1d1f`, 순수 검정이 아니다.

---

## 커밋

- **먼저 로컬에 쓰고, 내용을 확인받은 뒤에 커밋한다.** 파일 작성과 커밋을 한 번에 하지 않는다. 무엇이 들어가는지 눈으로 보고 나서 커밋한다.
- **메시지는 한국어로 쓴다.** 제목 한 줄 + 빈 줄 + 본문(왜 그렇게 했는지).
- **자기가 만든 파일만 명시적 경로로 스테이징한다.** `git add -A`는 진행 중인 다른 변경을 자기 커밋에 쓸어 담는다. 실제로 그럴 뻔한 적이 있다.
  - ⚠️ 구현계획서(`docs/plans/`)의 커밋 스텝 대부분이 `git add -A`로 적혀 있다. **계획서가 아니라 이 규칙을 따른다.**
- 작업 단위로 자주 커밋한다.
- Claude가 작성한 커밋에는 `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`을 넣는다.
- **커밋과 함께 회고를 쓴다.** (위 규칙 참고)

---

## 환경 — 함정이 있다

**Windows 11 / PowerShell.** Bash 툴도 쓸 수 있지만 셸이 다르다.

**Bash 툴에서 PowerShell here-string(`@'...'@`)을 쓰지 않는다.** `@` 문자가 그대로 들어가 커밋 메시지가 망가진다. 여러 줄 문자열이 필요하면 Bash heredoc(`<<'EOF'`)을 쓴다. 이 실수가 두 번 났다.

**PowerShell에서 네이티브 명령에 `2>&1`을 붙이지 않는다.** 정상 종료(exit 0)인데도 `NativeCommandError`가 뜬다. stderr는 이미 캡처된다.

**저장소가 OneDrive 폴더 안에 있다.** OneDrive가 `.git` 내부 파일을 클라우드 자리표시자로 비워버려 저장소가 통째로 망가진 적이 있다. git이 이상하면 이것부터 의심한다 — 회고 `2026-08-09_v01` 이슈 1.

**Docker가 없어서 `supabase start`(로컬 스택)를 못 쓴다.** 클라우드 프로젝트에 `npx supabase db push`로 마이그레이션을 올린다.

**스택 버전이 매우 최신이다.** Vite 8 / TypeScript 6 / React 19.2 / vitest 4 / Tailwind 4 / jsdom 29. 문제가 생기면 버전 차이를 먼저 의심한다. 스캐폴드는 ESLint가 아니라 **oxlint**를 쓴다.

---

## 자주 쓰는 명령

```bash
# 개발 서버
cd frontend && npm run dev          # http://localhost:5173

# 테스트
cd frontend && npm test             # 1회 실행
cd frontend && npm run test:watch   # 감시 모드

# 빌드 (타입 검사 포함)
cd frontend && npm run build

# Supabase 마이그레이션 적용
npx supabase db push

# DB 타입 재생성 (스키마 바꾼 뒤 반드시)
npx supabase gen types typescript --linked > frontend/src/lib/database.types.ts
```

## Supabase 프로젝트

| | |
|---|---|
| Project Ref | `mpcphuyvffsccsmrocwu` |
| Region | `ap-southeast-1` (싱가포르) |
| URL·anon key | `frontend/.env.local` (git 제외) |
